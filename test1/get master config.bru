meta {
  name: get master config
  type: http
  seq: 2
}

get {
  url: {{host}}/masters/config
  body: none
  auth: inherit
}
tests {
   test("get master config", function() {
      expect(res.status).to.equal(200);
    });
}
settings {
  encodeUrl: true
}
